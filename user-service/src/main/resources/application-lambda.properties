# User Service - Lambda Configuration
spring.application.name=user-service

# MongoDB Configuration - Usar variable de entorno
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://diegocardenasb_db_user:rPU4UBBESGQi4AQf@arep-twiter.dc0q3wo.mongodb.net/twitter_db?retryWrites=true&w=majority&appName=arep-twiter}
spring.data.mongodb.database=${MONGODB_DATABASE:twitter_db}

# AWS Cognito Configuration - Usar variables de entorno
spring.security.oauth2.resourceserver.jwt.issuer-uri=${COGNITO_ISSUER_URI:https://cognito-idp.us-east-1.amazonaws.com/us-east-1_3UE2M9esT}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${COGNITO_JWK_URI:https://cognito-idp.us-east-1.amazonaws.com/us-east-1_3UE2M9esT/.well-known/jwks.json}

# CORS Configuration - Actualizar con URL de CloudFront en producción
cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

# Logging
logging.level.com.arep.twitter=INFO
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=INFO

# Lambda specific optimizations
spring.main.lazy-initialization=true
